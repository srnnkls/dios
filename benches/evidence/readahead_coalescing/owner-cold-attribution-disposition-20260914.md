# Owner disposition: accept the cold CPU attribution limitation

Sören, via the sibling Claude session and explicit user instruction, 2026-09-14:
accept the recorded cold-attribution limitation and close RC6, with these conditions.

The exact retained candidate CPU-clock capture has **249 timed / 366 orphaned
samples**. Caller unwinding is incomplete, and the plan's unchanged **1% orphan
rule** withholds category CPU budgets. Preserve those null estimates and the
failed captures. Keep the measured cold totals, **1,472.5 ns elapsed / 1,464 ns
CPU per useful 4 KiB**, and the qualified candidate pressure attribution as the
RC-G6 record. No numeric gate changes; RC-G6 has no numeric adoption bound.

Carry the gap into the next **readahead-efficiency** scope, item 2 in
`/private/tmp/dios-steering-world-class.md`. That scope must obtain qualified cold
CPU attribution **before implementation** from a frame-pointer diagnostic replay.
The replay uses a **separate perturbed executable with its own identity**; it
must not be represented as the unchanged RC6 primary executable. More repetitions
cannot repair structural caller-unwinding loss. Its bench plan precedes the
diagnostic and any product code.

This disposition accepts the reporting limitation for RC6 closure. It does not
make any failed profile qualified, supply missing category estimates, change the
512 KiB default, or authorize a new RC6 timing campaign. Record the results,
README, checkpoint and adoption disposition, then complete the PR. Report the
final DRP-G4 confirmation geomean, upper and PR link.
